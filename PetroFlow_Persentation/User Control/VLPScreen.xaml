<UserControl x:Class="PetroFlow_Persentation.User_Control.VLPScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PetroFlow_Persentation.User_Control"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0">

        <!-- Row Definition -->
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="0.5*"/>
        </Grid.ColumnDefinitions>

        <!-- Top Left -->
        <Border Name="WellDataBorder"
                Grid.Row="0" 
                Grid.Column="0" 
                Style="{DynamicResource CardStyle}"
                Margin="0,0,10,10">
            <Grid Name="WellDataMainGrid">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Name="WellDataTitle"
                           Text="Well Data"
                           FontSize="20"
                           FontWeight="Black"
                           Foreground="{StaticResource Brush.Accent}"
                           Height="Auto"/>

                <Border Grid.Row="1"
                        Style="{StaticResource Separator}"/>

                <Grid Name="WellDataGrid"
                      Grid.Row="2">

                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Name="WellHeadPressureLabel"
                               Grid.Row="0"
                               Grid.Column="0"
                               Text="Well Head Pressure:"
                               />
                    <TextBlock Name="TubingSizeLabel"
                               Grid.Column="0"
                               Grid.Row="1"
                               Text="Tubing Size:"/>
                    <TextBlock Name="TotalDepthLable"
                               Grid.Column="0"
                               Grid.Row="2"
                               Text="Total Depth:"/>
                    <TextBlock Name="PipeRelativeRoughnessLabel"
                               Grid.Column="0"
                               Grid.Row="3"
                               Text="Pipe Relative Roughness:"
                               />

                </Grid>

            </Grid>
        </Border>

        <!-- Top Right -->
        <Border Grid.Row="0" 
                Grid.Column="1" 
                Style="{DynamicResource CardStyle}"
                Margin="10,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="General Control"
                           FontSize="20"
                           FontWeight="Black"
                           Foreground="{StaticResource Brush.Accent}"
                           Height="Auto"/>

                <Border Grid.Row="1"
                        Style="{StaticResource Separator}"/>

                <Grid Grid.Row="2">

                </Grid>

            </Grid>
        </Border>

        <!-- Bottom -->
        <Border Grid.Row="1" 
                Grid.Column="0" 
                Grid.ColumnSpan="2" 
                Style="{DynamicResource CardStyle}"
                Margin="0,10,0,0">
            <Grid>
            </Grid>
        </Border>


    </Grid>
</UserControl>
